import React from "react";
import Topbar from "../../layout/topbar";

const MenPage = ({ location }) => {
  return (
    <>
      <Topbar location={location} />
    </>
  );
};

export default MenPage;
